process ToChamberRefT0DB = {
   
    include "CondCore/DBCommon/data/CondDBSetup.cfi"
    include "Geometry/MuonCommonData/data/muonIdealGeometryXML.cfi"
    include "Geometry/DTGeometry/data/dtGeometry.cfi"
    include "Geometry/MuonNumbering/data/muonNumberingInitialization.cfi"

    source = EmptySource {}
    untracked PSet maxEvents = {untracked int32 input = 1}
   
    # VDrift, TTrig, TZero, Noise or channels Map from DB
    es_source calibDB = PoolDBESSource {  
	using CondDBSetup
	string connect = "sqlite_file:t0.db"
	untracked uint32 authenticationMethod = 0
	VPSet toGet = {{
	   	string record = "DTT0Rcd"
		string tag ="t0"
	    }}
    }
    
    service = PoolDBOutputService{
	using CondDBSetup
        string connect = "sqlite_file:tpDead.db"
	untracked string timetype = "runnumber"    
	untracked uint32 authenticationMethod = 0
	VPSet toPut={{
		string record = "DTDeadFlagRcd"
		string tag ="tpDead"
	    }}
    }
    
    
    #Module to dump DB to a file
    module dtTPDeadWriter = DTTPDeadWriter {
	untracked bool debug = true
    }	
    
    path p = {dtTPDeadWriter}

}
