process DTVDRIFTANALYZER = {
   
    include "CondCore/DBCommon/data/CondDBSetup.cfi"
    include "Geometry/MuonCommonData/data/muonIdealGeometryXML.cfi"
    include "Geometry/DTGeometry/data/dtGeometry.cfi"
   
    source = EmptySource{}
    untracked PSet maxEvents = {untracked int32 input = 1}
    

    es_source vdrift = PoolDBESSource {  
	using CondDBSetup
	string connect =  "sqlite_file:vdrift.db"
	untracked string catalog = "file:vdrift.xml"
	string timetype = "runnumber"    
	untracked uint32 authenticationMethod = 0
	VPSet toGet = {{
		string record = "DTMTimeRcd"
                string tag ="vdrift"
	    }}
    }
  
  
    #Module to produce t0 histograms and save them into a root file
    module dtVDriftAnalyzer = DTVDriftAnalyzer {
	untracked string rootFileName = "vdriftHistos.root"
    }	
    
    path p = {dtVDriftAnalyzer}

}
