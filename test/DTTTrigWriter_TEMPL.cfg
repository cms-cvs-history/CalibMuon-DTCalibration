process PROD = {
    
     source = EmptySource{}
    untracked PSet maxEvents = {untracked int32 input = 1}
  
    include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"
    include "Geometry/MuonCommonData/data/muonIdealGeometryXML.cfi"
    include "Geometry/DTGeometry/data/dtGeometry.cfi"
    include "Geometry/MuonNumbering/data/muonNumberingInitialization.cfi"

    #Service to write in the DB
    service = PoolDBOutputService{
	string connect = "sqlite_file:/afs/cern.ch/cms/CAF/CMSALCA/ALCA_MUONCALIB/DTCALIB/CRUZET/ttrig/ttrig_first_TEMPLATE.db"
	untracked string timetype = "runnumber"    
	untracked uint32 authenticationMethod = 0
	PSet DBParameters = {}
	VPSet toPut={{
		string record = "DTTtrigRcd"
		string tag ="ttrig"
	    }}
    }
    
    #Module to compute ttrig
    module ttrigwriter = DTTTrigWriter {
	# Switch on/off the verbosity
	untracked bool debug = true
	# Name of the input ROOT file which contains the time boxes
	untracked string rootFileName = "/afs/cern.ch/cms/CAF/CMSALCA/ALCA_MUONCALIB/DTCALIB/CRUZET/ttrig/DTTimeBoxes_TEMPLATE.root"
    }	
    
    path p = {ttrigwriter}

}

