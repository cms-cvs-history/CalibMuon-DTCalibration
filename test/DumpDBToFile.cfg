process DumpDBToFile = {
	source = EmptySource {untracked int32 maxEvents = 1}

	# TTrig from DB
	es_source ttrig = PoolDBESSource {  VPSet toGet = {
                                    {string record = "DTTtrigRcd" 
                                     string tag = "ttrig_test"
                                     } }
		    		     bool loadAll = true
                                     string connect = "sqlite_file:ttrig_fromFile.db"
                                     untracked string catalog = "file:ttrig_fromFile.xml"
			             string timetype = "runnumber" 
				     untracked uint32 authenticationMethod = 0		
                                     untracked uint32 messagelevel = 0
				    }




	module dumpToFile = DumpDBToFile {
		untracked string outputFileName = "Test.txt"
		untracked PSet calibFileConfig = {
		}
	}	

	path p = {dumpToFile}

}
