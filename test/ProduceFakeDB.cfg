process ProdFakeDB = {
    
    include "Geometry/MuonCommonData/data/muonIdealGeometryXML.cfi"
    include "Geometry/DTGeometry/data/dtGeometry.cfi"
      
    include "CondCore/DBCommon/data/CondDBSetup.cfi"
  
     source = EmptySource{}
    untracked PSet maxEvents = {untracked int32 input = 1}
   
    # VDrift, TTrig, TZero into DB
    service = PoolDBOutputService{
	using CondDBSetup
        string connect = "sqlite_file:tzero.db"
 	untracked string catalog = "file:tzero.xml"
	string timetype = "runnumber"    
	untracked uint32 authenticationMethod = 0
	VPSet toPut={{
		# VDrift
	   	#string record = "DTMtimeRcd"
		#string tag ="vdrift"
		# TZero
		string record = "DTT0Rcd"
		string tag ="tzero"
		# TTrig
		#string record = "DTTtrigRcd"
		#string tag ="tTrig"
	    }}
    }
    
    #Module to produce a fake in a .txt file format
    module prodFakeDb = ProduceFakeDB {
	
    	#Choose what database you want to write
	#untracked string dbToProduce = "VDriftDB"
	untracked string dbToProduce = "TZeroDB"
	#untracked string dbToProduce = "TTrigDB"
  	#untracked string dbToProduce = "NoiseDB"

	#Choose the fake values
	#TTrig
	#untracked string mapToProduce = "TTrigDB"
	#untracked double ttrig = 496
	#untracked double sigmaTtrig = 0
	
	#VDrift
	#untracked string mapToProduce = "VDriftDB"
	#untracked double vdrift = 0.00543
	#untracked double hitResolution = 0.02
	
	#TZero
   	untracked string mapToProduce = "TZeroDB"
	untracked double tzero = 0
	untracked double sigmaTzero = 0

    }	
    
    path p = {prodFakeDb}

}
